cmake_minimum_required(VERSION 3.10)

# add custom project to solution
set(PYTEST_FULL_PROJECT_FILE "${CMAKE_CURRENT_SOURCE_DIR}/pytests.pyproj")
get_filename_component(PYTEST_PROJECT_NAME ${PYTEST_FULL_PROJECT_FILE} NAME_WE)
INCLUDE_EXTERNAL_MSPROJECT(${PYTEST_PROJECT_NAME} ${PYTEST_FULL_PROJECT_FILE} ${BINDINGS_NAME})
